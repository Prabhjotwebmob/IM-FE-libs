import { Injectable } from "@angular/core";

@Injectable({
    providedIn: "root"
})
export class ToastrService {
    constructor() { }

    show(message, type) {
        // will show toaster
    }
}
